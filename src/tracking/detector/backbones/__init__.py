"""Vision backbones for the detector.

`mvq/model.py` constructs `DINOv3`/`DINOv3Config` directly, so nothing here is
needed for the MVQ checkpoint contract. This package intentionally re-exports
nothing until something needs it.
"""
