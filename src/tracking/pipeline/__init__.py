"""Stage wiring: the registry, resume, timings and the stage callables.

This package adds no numerics. Everything it runs lives in `preprocess/`,
`inverse_kinematics/`, `postprocess/`, `qc/` and `detector/`; this is the layer
that decides WHAT to run and WHETHER it still needs running.
"""
